<Window x:Class="CrazyZoo.AddAnimalWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            mc:Ignorable="d"
            xmlns:resx="clr-namespace:CrazyZoo"

            Title="AddAnimal" Height="200" Width="450">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Type: " Grid.Row="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="TypeComboBox" Grid.Row="0" Grid.Column="1" Width="200"/>

        <TextBlock Text="Name: " Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox x:Name="NameTextBox" Grid.Row="1" Grid.Column="1" Width="200"/>

        <TextBlock Text="Age: " Grid.Row="2" VerticalAlignment="Center"/>
        <TextBox x:Name="AgeTextBox" Grid.Row="2" Grid.Column="1" Width="200"/>

        <TextBlock Text="Crazy action: " Grid.Row="3" VerticalAlignment="Center"/>
        <TextBox x:Name="CrazyActionTextBox" Grid.Row="3" Grid.Column="1" Width="200"/>

        <Button Grid.Row="4" Grid.ColumnSpan="2" Height="35"
                    Margin="115,30,115,0"
                    Content="Add animal" Click="AddButton_Click"/>
    </Grid>
</Window>
